# these processes should be how the client interacts with the document, with a proxy being spawned for every client
# these will need their own supervisior & registry as well
defmodule Nc.Workers.ClientProxy do
end
